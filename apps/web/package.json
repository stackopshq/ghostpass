{
  "name": "@ghostpass/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Web app GhostPass (SPA Svelte) — coffre zero-knowledge, crypto côté client (WASM)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "@simplewebauthn/browser": "^13.3.0",
    "ghostpass-crypto-wasm": "file:../../crates/ghostpass-crypto-wasm/pkg"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tsconfig/svelte": "^5.0.0",
    "svelte": "^5.15.0",
    "svelte-check": "^4.1.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
